import { TipoClientePipe } from './tipo-cliente-pipe';

describe('TipoClientePipe', () => {
  it('create an instance', () => {
    const pipe = new TipoClientePipe();
    expect(pipe).toBeTruthy();
  });
});
