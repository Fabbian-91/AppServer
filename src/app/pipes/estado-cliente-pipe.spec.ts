import { EstadoClientePipe } from './estado-cliente-pipe';

describe('EstadoClientePipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoClientePipe();
    expect(pipe).toBeTruthy();
  });
});
